# Decisions Log

Append-only log of decisions reached by both GPT/Codex and Claude in
`LIVE_DIALOGUE.md`. Each entry is added only after **both** sides have
written explicit approval (GPT_FINAL_APPROVAL + CLAUDE_APPROVAL) on the
matching scope.

Entry format:

```
### [YYYY-MM-DD] <short title>
- Decision: <one sentence>
- Scope: <what is in / out>
- Source: LIVE_DIALOGUE.md GPT_TURN_X + CLAUDE_TURN_X
- Why: <brief>
```

---

(no decisions reached yet — dialogue is on GPT_TURN_1 / CLAUDE_TURN_1
with conditional approval pending GPT_TURN_2 response)
